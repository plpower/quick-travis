language: node_js
node_js:
  - "stable"
cache:
  directories:
  - node_modules
script:
  # - yarn test
  - yarn build
before_install:
  - cd src
after_success:
  - yarn add -g firebase-tools
  - firebase deploy --token $FIREBASE_TOKEN --non-interactive